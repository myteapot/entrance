# Cold Layer

> Status: repo-side cold staging

This tree holds subordinate documents that remain useful but should not sit on the hot top surface.

- stable subordinate specs belong here
- the DB archival pass is still pending
- `TODO(fill)`: add explicit `supersedes / conflicts_with / derived_from` links during DB archival
