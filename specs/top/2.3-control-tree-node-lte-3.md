# 2.3 Control Tree (Node <= 3) Design

> Status: hot skeleton

## Purpose

- define the top control-tree document as part of the first design batch
- define the routing/topology side without collapsing into UI detail
- hold the place for the `Node <= 3` control-tree constraint line

## Confirmed Oracle Points

- A tree-level control/routing design should be written as a separate top document.
- `2.3 Control Tree / Bt tree (Node <= 3)` belongs to the first batch of top design documents.
- `Node <= 3` remains the active constraint marker for this design line.
- This document should stay above specific UI or implementation details.

## Current Boundary Reading

- this document is about control topology rather than front-end surface detail
- the exact internal naming can remain open while the top document boundary is fixed
- routing/tree semantics should stay separated from lower execution detail

## Mounted Cold Docs

- [front.md](../cold/2.3-control-tree-node-lte-3/front.md)

## Mounted Chore Docs

- `TODO(fill)`

## TODO(fill)

- define node kinds and tree semantics
- define relation between tree shape, routing, and parallel lanes
- decide whether the naming should remain `Bt tree` or be normalized later
