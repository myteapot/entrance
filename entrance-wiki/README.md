# Entrance Wiki

This directory contains committed Markdown project knowledge.

Start from `current/`. Everything under `archive/` is historical context and
must not override the active source shape in `../entrance-src/`.
