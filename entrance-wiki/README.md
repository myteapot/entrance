# Entrance Wiki

This directory contains committed Markdown project knowledge.

Use it for orientation and project history, then verify implementation claims
against `../entrance-src/` before changing product code. Documents that mention
`harness/`, `shell/cli/`, `shell/mcp/`, `hosts/desktop/tauri/`, V0, or V1 may be
historical context rather than the active V2 source shape.
